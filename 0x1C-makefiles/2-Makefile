
# the compiler to use

CC = gcc

# the source files

SRC = $(wildcard *.c)

# the object files

OBJ = $(SRC:.c=.o)

# the name of the executable

NAME = school

# The default rule

all: $(OBJ)
	$(CC) $(OBJ) -o $(NAME)
